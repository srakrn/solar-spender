{
  "name": "solar-spender-panel",
  "private": true,
  "type": "module",
  "version": "0.6.1",
  "scripts": {
    "build": "esbuild src/solar-spender-panel.js --bundle --format=esm --target=es2022 --loader:.css=text --outfile=../custom_components/solar_spender/frontend/solar-spender-panel.js",
    "test": "node --test src/*.test.js"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
